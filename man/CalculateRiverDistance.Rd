% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/function_CalculateRiverDistance.R
\name{CalculateRiverDistance}
\alias{CalculateRiverDistance}
\title{Calculate River Distance Between Two SUBIDs}
\usage{
CalculateRiverDistance(SUBID1, SUBID2, gd, bd = NULL)
}
\arguments{
\item{SUBID1}{Numeric or character. The first SUBID.}

\item{SUBID2}{Numeric or character. The second SUBID.}

\item{gd}{A data frame containing HYPE geographical data (i.e. an imported 'GeoData.txt' file). Must contain the
columns \code{SUBID}, \code{MAINDOWN}, and \code{RIVLEN}.}

\item{bd}{Optional data frame containing HYPE branch data. Used by
\code{\link[=AllDownstreamSubids]{AllDownstreamSubids()}} to determine the routing network. Default is
\code{NULL}.}
}
\value{
A numeric value giving the river distance between \code{SUBID1} and \code{SUBID2}.
The value is positive when \code{SUBID1} is upstream of \code{SUBID2} and negative when
\code{SUBID1} is downstream of \code{SUBID2}. Returns \code{NA} if no routing connection
exists between the two SUBIDs.
}
\description{
Calculates the river distance between two SUBIDs along the routing network.
The distance is calculated by summing the \code{RIVLEN} values of the subbasins
between the two requested SUBIDs. The sign of the returned distance indicates
the direction of the routing: positive if \code{SUBID1} is upstream of \code{SUBID2},
and negative if \code{SUBID1} is downstream of \code{SUBID2}.
}
\details{
The function first checks that both requested SUBIDs exist in \code{gd} and that
the \code{RIVLEN} column is available. It then uses \code{\link[=AllDownstreamSubids]{AllDownstreamSubids()}} to
identify the downstream routing from the first SUBID.

If \code{SUBID2} is downstream of \code{SUBID1}, the distance is returned as a positive
value. If \code{SUBID1} is downstream of \code{SUBID2}, the routing direction is
reversed and the distance is returned as a negative value.

The distance is calculated by summing the \code{RIVLEN} values for the SUBIDs
between the two requested SUBIDs. The \code{RIVLEN} of the upstream SUBID itself
is not included in the calculation.

If no routing connection exists between the two SUBIDs, the function issues
a warning and returns \code{NA}.
}
\examples{
# Read GeoData and BranchData files
gd <- ReadGeoData(
  filename = system.file("demo_model", "GeoData.txt", package = "HYPEtools")
)
bd <- ReadGeoData(
  filename = system.file("demo_model", "GeoData.txt", package = "HYPEtools")
)

# Calculate the river distance between two SUBIDs
CalculateRiverDistance(
  SUBID1 = 3344,
  SUBID2 = 3587,
  gd = gd
)

}
\seealso{
\code{\link[=AllDownstreamSubids]{AllDownstreamSubids()}}
\code{\link[=AllUpstreamSubids]{AllUpstreamSubids()}}
}
